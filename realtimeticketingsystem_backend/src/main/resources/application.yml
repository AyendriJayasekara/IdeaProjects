
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ticketing_system
    username: root
    password: Ayendri@2003

  spring:
    jpa:
      show-sql: true
      properties:
        hibernate:
          format_sql: true

  logging:
    level:
      org.hibernate.SQL: DEBUG
      org.hibernate.type.descriptor.sql.BasicBinder: TRACE





